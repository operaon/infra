# Este arquivo alimenta SOMENTE interpolações dentro do docker-compose.yml
# (variáveis ${...} usadas no próprio YAML, ex: comando do Redis).
# As credenciais de banco e demais segredos da API vêm de
# ../../velyon_api/.env via env_file — não duplicar aqui.

REDIS_PASSWORD=qoRufVQAZqOQ5Bnrn5VzNkQjF4sjlBef
