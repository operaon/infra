# ==================== STAGE 1: BUILDER ====================
FROM nginx:alpine AS builder

RUN rm -f /etc/nginx/conf.d/default.conf

# ==================== STAGE 2: RUNTIME ====================
FROM nginx:alpine

LABEL maintainer="admin@velyonrobotics.com"
LABEL version="1.0.0"
LABEL description="Velyon - Nginx Reverse Proxy (API + Frontend)"
LABEL org.opencontainers.image.source="https://github.com/velyon-homolog/velyon_infra"

COPY nginx/nginx.conf /etc/nginx/nginx.conf
COPY nginx/conf.d     /etc/nginx/conf.d

# Os certificados NÃO são copiados para dentro da imagem.
# Eles chegam em tempo de execução via volume certbot_certs
# montado em /etc/letsencrypt no docker-compose.
# O nginx.conf aponta para /etc/letsencrypt/live/velyonrobotics.com/

RUN mkdir -p /var/www/certbot /var/log/nginx && \
    rm -f /etc/nginx/conf.d/default.conf

EXPOSE 80 443

HEALTHCHECK --interval=30s --timeout=10s --start-period=15s --retries=3 \
    CMD wget --quiet --tries=1 --spider http://localhost/nginx-health || exit 1