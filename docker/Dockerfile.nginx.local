# ==================== STAGE 1: BUILDER ====================
FROM nginx:alpine AS builder

RUN rm -f /etc/nginx/conf.d/default.conf

# ==================== STAGE 2: RUNTIME ====================
FROM nginx:alpine

LABEL maintainer="admin@velyonrobotics.com"
LABEL version="1.0.0"
LABEL description="Velyon - Nginx Reverse Proxy (Local Dev — sem SSL)"
LABEL org.opencontainers.image.source="https://github.com/velyon-homolog/velyon_infra"

# Reaproveita o mesmo nginx.conf de produção (sem nada específico de
# domínio/SSL nele) e troca apenas o conf.d pelo conjunto local,
# que roteia por porta em vez de por domínio.
COPY nginx/nginx.conf      /etc/nginx/nginx.conf
COPY nginx/conf.d.local    /etc/nginx/conf.d

RUN mkdir -p /var/log/nginx && \
    rm -f /etc/nginx/conf.d/default.conf

EXPOSE 80 8081

HEALTHCHECK --interval=30s --timeout=10s --start-period=15s --retries=3 \
    CMD wget --quiet --tries=1 --spider http://localhost/nginx-health || exit 1
